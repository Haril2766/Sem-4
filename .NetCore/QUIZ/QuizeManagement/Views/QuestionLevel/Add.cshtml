@model QuizeManagement.Models.QuestionLevelModel

@{
    ViewBag.Title = "Create Question Level";
}


<main id="main" class="main">
    <div class="container">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">Add Question Level</h5>
                @* <form class="row g-3"> *@
                @*     <div class="col-12"> *@
                @*         <label for="inputquesation4" class="form-label" placeholder="User Id"> *@
                @*             <span class="text-danger">*</span> *@
                @*             User  ID</label> *@
                @*         <select id="inputState" class="form-select"> *@
                @*             <option selected disabled>User ID</option> *@
                @*         </select> *@
                @*     </div> *@
                @*     <div class="col-12"> *@
                @*         <label for="inputquesation4" class="form-label"> *@
                @*             <span class="text-danger">*</span> *@
                @*             Question Level</label> *@
                @*         @* <input type="text" class="form-control" id="inputquesation4"> *@ 
                @*         <input type="text" class="form-control  " required placeholder="Question Level"> *@
                @*     </div> *@
                  
                @*     <div class="text-center"> *@
                @*         <button type="submit" class="btn btn-primary">Save</button> *@
                @*         <a class="btn btn-secondary" asp-controller="QuestionLevel" asp-action="List">Cancel</a> *@
                @*     </div> *@
                @* </form><!-- Vertical Form --> *@

                @using (Html.BeginForm("Create", "QuestionLevel", FormMethod.Post))
                {
                    <div class="row g-3">
                        <div class="col-12">
                            <span class="text-danger">*</span>
                            @Html.LabelFor(QuestionLevelModel => QuestionLevelModel.UserId, "User ID", new { @class = "form-label" })
                            @Html.DropDownListFor(QuestionLevelModel => QuestionLevelModel.UserId, new SelectList(new List<SelectListItem> { })," Enter User Id", new { @class = "form-select", required = "required", placeholder = ""   })
                            @Html.ValidationMessageFor(QuestionLevelModel => QuestionLevelModel.UserId, "", new { @class = "text-danger" })
                        </div>

                        <div class="col-12">
                            <span class="text-danger">*</span>
                            @Html.LabelFor(QuestionLevelModel => QuestionLevelModel.Level, "Question Level", new { @class = "form-label" })
                            @Html.TextBoxFor(QuestionLevelModel => QuestionLevelModel.Level, new { @class = "form-control", placeholder = "Enter Question Level", required = "required" })
                            @Html.ValidationMessageFor(QuestionLevelModel => QuestionLevelModel.Level, "", new { @class = "text-danger" })
                        </div>

                        <div class="text-center">
                            <button type="submit" class="btn btn-primary">Save</button>
                            <a class="btn btn-secondary" href="@Url.Action("List", "QuestionLevel")">Cancel</a>
                        </div>
                    </div>
                }

            </div>
        </div>
    </div>
</main>
 